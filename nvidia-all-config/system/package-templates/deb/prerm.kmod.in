#!/bin/sh
set -e
case "$1" in
  remove|upgrade|deconfigure)
    for moddir in /lib/modules/*; do
      [ -d "$moddir" ] || continue
      kver=${moddir##*/}
      depmod -a "$kver" 2>/dev/null || true
    done
    ;;
esac
